import {FormEvent,useState} from 'react'; import {useNavigate} from 'react-router-dom'; import {LogIn,AlertCircle} from '../components/Icons'; import {Layout} from '../components/Layout'; import {login} from '../lib/api';
export function LoginPage(){const nav=useNavigate();const[email,setEmail]=useState('');const[pw,setPw]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();try{setBusy(true);setError('');await login(email,pw);nav('/admin')}catch{setError('Anmeldung fehlgeschlagen. Bitte E-Mail und Passwort prüfen.')}finally{setBusy(false)}}return <Layout><div className="auth-page"><form className="auth-card" onSubmit={submit}><div className="eyebrow"><LogIn size={16}/> ADMIN-LOGIN</div><h1>Willkommen zurück.</h1><p>Nur der festgelegte Administrator kann Teilnehmer und Ziehungen verwalten.</p>{error&&<div className="alert error"><AlertCircle size={18}/>{error}</div>}<label>E-Mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="email"/></label><label>Passwort<input type="password" value={pw} onChange={e=>setPw(e.target.value)} required autoComplete="current-password"/></label><button className="primary full" disabled={busy}>{busy?'Anmelden…':'Anmelden'}</button><a className="back-link" href="/">← Zur öffentlichen Seite</a></form></div></Layout>}
